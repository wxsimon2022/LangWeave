"""Business workflow handlers (batch jobs, event-driven flows).

Add modules here when logic is more than a single service call, e.g.:

- `app/handlers/order_handler.py` — post-processing after agent tool calls
- `app/handlers/report_handler.py` — scheduled report generation
"""
