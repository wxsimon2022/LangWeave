## AGENTS.md metadata - UI listing info
# Generated by codex agent during skill creation
---
interface:
  display_name: LangWeave (织语)
  short_description: LangChain 1.x / LangGraph 多 Agent 框架与聊天应用
  default_prompt: >-
    Use the LangWeave skill when working in the langchain project.
    This skill covers AgentBuilder, intent routing, supervisor pattern,
    SSE streaming, tool registry, MCP, RAG, conversation memory, and
    the FastAPI web layer with JWT auth and rate limiting.
